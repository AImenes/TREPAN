class Node:
    def __init__(self, net, data, X, y):
        self.net = net
        self.data = data
        self.X
        self.y

    #(i) to determine the class labels for the network's training examples;
    def determine_class_labels(self):
        return None

    # (ii) to select splits for each of the tree's internal nodes;

    # iii) and to determine if a node covers instances of only one class.